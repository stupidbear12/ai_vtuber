# AI 버튜버 시온(sion) — 전체 모듈 통합 의존성
# 개별 모듈은 modules/{name}/requirements.txt 참조
# 이 파일은 모든 모듈 의존성을 한 번에 설치할 때 사용합니다:
#   pip install -r requirements.txt

# ── 공통 (전 모듈) ─────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-dotenv>=1.0.0

# ── ai_vtuber_core, ai_broadcast, ai_chat — HTTP 클라이언트 ────
aiohttp>=3.9.0

# ── ai_live2d — 정적 파일 서빙 ─────────────────────────────────
python-multipart>=0.0.6
aiofiles>=23.1.0
websockets>=11.0

# ── ai_broadcast — 유튜브/치지직 채팅 수집 ─────────────────────
pytchat>=0.5.5
# websockets 이미 위에 포함

# ── ai_voice — GPT-SoVITS API 클라이언트 ────────────────────────
httpx>=0.27.0
